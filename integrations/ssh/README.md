
# SSH Askpass Integration

## Purpose
Provides a modern GTK-based SSH passphrase helper with legacy X11 fallback.

## Usage
Enable the integration by sourcing the selector script:
```bash
source integrations/ssh/askpass/selector.sh
